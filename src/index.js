import React from 'react';
import ReactDOM from 'react-dom/client';
import { EmogisApp } from './Emogis';
import './styles.css'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <EmogisApp />
  </React.StrictMode>
);


